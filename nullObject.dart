//When you define a variable or object or didn't assign any value or variable then it means it is empty and contain a null object.
main(){
  var num1; //Didn't assigned value
  print(num1);

  var num2 = null; //Assigned value
  print(num2);
}